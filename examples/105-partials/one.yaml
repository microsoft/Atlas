
operations:
- message: this is a nested operation in the one.yaml partial
- operations:
  - message: foo is {{foo}}
  - message: bar.name is {{bar.name}}
  - message: bar.color is {{bar.color}}
- message: "the current context is: {{json}}"
- message: "the parent context is: {{json ..}}"
